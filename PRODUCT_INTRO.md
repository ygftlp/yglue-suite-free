# Ygflow Suite 产品介绍（内部版）

## 背景与定位
- 面向企业内部服务编排与治理需求，帮助研发团队在既有微服务体系之上快速搭建业务流程。
- 核心目标是让开发人员聚焦业务规则与数据转换，尽量减少脚手架与对接类重复劳动。
- 仅供公司内部团队使用，不涉及对外商业化交付或收费功能。

## 设计原则
- **业务优先**：所有功能围绕“快速表达业务逻辑”展开，避免使用者陷入底层实现细节。
- **可视化 + DSL 双轨**：提供可视化编辑体验，同时保留标准化 DSL/JSON 资产，便于审计与协作。
- **开放兼容**：兼容公司现有认证、网关、配置中心等基础设施，不强依赖任何商业化组件。
- **可治理**：流程、参数、脚本统一管理，支持版本追踪与变更审计，满足内部治理要求。

## 核心能力
- **流程编排**：以拖拽画布方式构建入口、任务、分支、事务、转换等节点，并支持节点复用。
- **数据转换**：内置 Transformer Editor，实现字段映射、集合处理、条件表达式与 Groovy 扩展脚本。
- **参数解析**：Param Resolver 统一维护上下文参数及其来源，支持多场景复用。
- **调试验证**：支持在线预览、断点式模拟、调用链追踪，确保流程在发布前可控。
- **版本管理**：提供流程版本比对、发布记录、回滚等治理能力。
- **IDE 协同**：配套 IntelliJ 插件，实现平台配置与本地开发之间的同步与自动提醒。

## 平台组成
- **Orchestrator UI**：浏览器端可视化工作台，流程建模、调试与发布的主入口。
- **Orchestrator 服务层**：承载流程元数据管理、运行时调度、权限控制等后端能力。
- **Runtime 模块**：执行流程与参数解析的运行时引擎，可集成在业务服务或统一网关内。
- **IDEA 插件**：为内部开发人员提供离线编辑、元数据同步与自动巡检能力。
- 各组件均围绕内部私有化部署设计，支持通过 CI/CD 流程统一发布。

## 典型应用场景
- 聚合多个后端服务形成统一接口，减少前端或上游系统调用复杂度。
- 构建规则驱动的业务流程，例如审批流、风控策略、订单补偿等。
- 搭建老系统与新平台之间的适配层，通过脚本与转换器实现快速兼容。
- 支撑灰度发布、AB 测试等场景，通过条件和分支节点控制流向。

## 与现有体系的关系
- 支持通过内网认证服务完成登录与鉴权，可配置不同环境下的网关地址。
- 可输出标准 REST 接口，也能与消息、事件系统结合；流程运行过程中遵循公司安全规范。
- 所有配置与脚本均存储在内部 Git/数据库中，可与现有审计、监控、告警体系集成。

## 合规与边界
- 产品仅面向公司内部研发团队，不提供对外 SaaS、PaaS 能力。
- 平台产生的数据与脚本须遵守公司安全与合规要求，涉及敏感信息需按照等级保护处理。
- 使用者应通过代码评审、流程发布审批等既有内控机制保证上线质量。

---

此文档为内部资料，请勿对外传播。

