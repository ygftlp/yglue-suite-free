<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yglue.flow.orch.persistence.mapper.FlowSyncStateMapper">

    <resultMap id="FlowSyncStateResultMap" type="org.yglue.flow.orch.domain.FlowSyncState">
        <id property="id" column="id"/>
        <result property="projectId" column="project_id"/>
        <result property="flowId" column="flow_id"/>
        <result property="instanceKey" column="instance_key"/>
        <result property="lastVersionId" column="last_version_id"/>
        <result property="lastSyncedAt" column="last_synced_at"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
    </resultMap>

    <select id="selectByFlowAndInstance" resultMap="FlowSyncStateResultMap">
        SELECT *
        FROM yglue_flow_sync_state
        WHERE flow_id = #{flowId}
          AND instance_key = #{instanceKey}
          AND del_flag = 0
    </select>

    <select id="selectByProjectAndInstance" resultMap="FlowSyncStateResultMap">
        SELECT *
        FROM yglue_flow_sync_state
        WHERE project_id = #{projectId}
          AND instance_key = #{instanceKey}
          AND del_flag = 0
    </select>

    <insert id="insert" parameterType="org.yglue.flow.orch.domain.FlowSyncState" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO yglue_flow_sync_state
        (project_id, flow_id, instance_key, last_version_id, last_synced_at, create_by, update_by)
        VALUES (#{projectId}, #{flowId}, #{instanceKey}, #{lastVersionId}, #{lastSyncedAt}, #{createBy}, #{updateBy})
    </insert>

    <update id="update" parameterType="org.yglue.flow.orch.domain.FlowSyncState">
        UPDATE yglue_flow_sync_state
        SET last_version_id = #{lastVersionId},
            last_synced_at = #{lastSyncedAt},
            update_by = #{updateBy}
        WHERE id = #{id}
    </update>

</mapper>
