<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yglue.flow.orch.persistence.mapper.FlowVersionMapper">

    <resultMap id="FlowVersionResultMap" type="org.yglue.flow.orch.domain.FlowVersion">
        <id property="id" column="id"/>
        <result property="flowId" column="flow_id"/>
        <result property="versionNo" column="version_no"/>
        <result property="contentJson" column="content_json"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="FlowVersionColumns">
        id, flow_id, version_no, content_json, create_by, update_by, create_time, update_time, del_flag
    </sql>

    <insert id="insert" parameterType="org.yglue.flow.orch.domain.FlowVersion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO yglue_flow_version (flow_id, version_no, content_json, create_by, update_by)
        VALUES (#{flowId}, #{versionNo}, #{contentJson}, #{createBy}, #{updateBy})
    </insert>

    <select id="selectByFlowIdDesc" resultMap="FlowVersionResultMap">
        SELECT <include refid="FlowVersionColumns"/>
        FROM yglue_flow_version
        WHERE flow_id = #{flowId}
          AND del_flag = 0
        ORDER BY version_no DESC
    </select>

    <select id="selectByFlowIdAndVersion" resultMap="FlowVersionResultMap">
        SELECT <include refid="FlowVersionColumns"/>
        FROM yglue_flow_version
        WHERE flow_id = #{flowId}
          AND version_no = #{versionNo}
          AND del_flag = 0
    </select>

    <select id="selectMaxVersion" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(version_no), 0)
        FROM yglue_flow_version
        WHERE flow_id = #{flowId}
          AND del_flag = 0
    </select>

    <select id="selectByIds" resultMap="FlowVersionResultMap">
        SELECT <include refid="FlowVersionColumns"/>
        FROM yglue_flow_version
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND del_flag = 0
    </select>

    <update id="updateContentJson">
        UPDATE yglue_flow_version
        SET content_json = #{contentJson},
            update_by = #{updateBy}
        WHERE id = #{id}
          AND del_flag = 0
    </update>

</mapper>
