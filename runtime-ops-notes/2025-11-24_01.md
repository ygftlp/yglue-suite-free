# 2025-11-24 晚间处理计划（问题序号 01）

## LiteFlow 规则热更新
- 为 `FlowDefinitionToLiteFlowConverter` 增加链签名缓存（`chainId -> checksum`），内容变化即触发重新注册。
- `registerComponent` 记录 `chainId -> componentId` 映射，`unregister` 时清空旧节点，避免静态 Map 泄漏。
- 发布流程成功后调用运行时刷新接口，确保 LiteFlow 链与数据库版本同步。
- 完成后补上 `clearAll()` 实际卸载 LiteFlow 链逻辑。

## Transformer 输入性能
- 在 `FlowExecutor` 中新增 `_lastNodeId`、`_node_{id}` 结果索引，Transformer 直接按前驱节点取值。
- 流程保存阶段为 Transformer 自动写入 `inputSource`（前驱唯一时指向 `_node_sourceId`），减少运行时回退。
- 历史数据 fallback 仅保留 `_lastNodeResult`/`ret`，移除对全量上下文的遍历，并追加命中日志，提示缺失配置。
- 长远看为 `FlowContext` 增设 `nodeResults` 专用 Map，ParamResolver 新增 `type: "node"` 读取指定节点输出。

## 协同事项
- 与前端确认 `inputSource` 自动写入规则，避免节点重排导致引用错误。
- 评估运行时刷新接口的幂等与权限（是否仅限发布操作调用）。
- 补充单元测试覆盖：链重新注册、Transformer 输入命中顺序、fallback 告警。

> 以上任务计划今晚统一处理，请在推送前同步 CI 状态。
